# 执行记录

状态：工程阶段已形成可检查实现；完整普查、正式入库、合并与线上发布尚未完成。

## 基线与隔离

- 原工作副本保持在原 main，不切换、不覆盖已有原件。
- 独立分支：`codex/digital-sztu-upgrade`。
- 基线：`61db849940009537f3ee2a5c0b392109096863a0`，包含用户最新 README 排版。
- 原研究任务「深挖 SZTU 相关项目并建 Issue」的输出与队列已读取并迁移；该任务没有待合并的独立代码 PR。

## 工程检查点

- 统一 Digital SZTU 包、插件、主命令和 Python 模块，保留旧命令、模块与初始化 Skill。
- 增加 Knowledge Record 与共享证据 Schema，继续读取 v0.1。
- 统一公开筛选生成图谱数据、SVG、Markdown 阅读目录和自包含查看器；修正 README 对标题层级、引文块和强调的过度限制。
- 新增 SQLite 研究状态、分页事务、只读来源发现、人工复核、定向补漏和受门槛约束的可恢复入库。
- 配置仅在主分支全部验证通过后发布的 Pages Actions；暂未更名仓库、合并或发布。
- 三栏概念图只存为未来需求，图中的占位内容不入库。

## 已执行验证

- 全套 175 项测试运行通过，2 项平台集成测试默认跳过；另外显式开启 macOS 核心集成，38 项安装相关测试全部通过，包括真实首次安装、重复运行、只读检查、中断恢复及手工改动保护。Windows 仍等待 CI 验证。
- 新旧命令 doctor、旧模块 validate 通过。
- 隔离样例检验未知有效期、同名对象、跨记录导航、反证、隐私排除和两次构建字节一致。
- 研究测试检验稳定 ID、桥接边界、分页重放、中断恢复、fork 贡献者范围、搜索截断和不完整普查禁止入库。
- 入库测试检验重复执行、保留旧文件、隔离校验失败与文件／映射异常回滚。
- 文本扫描覆盖 SVG／CSS／JS，超过旧 8 MiB 阈值的文本也逐行读取；回归测试确认大文件尾部的凭据模式仍会被发现，报告不回显值。
- Skill frontmatter 校验通过。验证工具使用的 PyYAML 仅安装在任务虚拟环境，不增加产品依赖。
- Pages 暂存生成通过；清单只允许自包含 HTML、SVG 与版本清单。
- 通过只返回隔离生成 HTML 的本地 HTTP 页面，实际检查中文搜索、无匹配、类型筛选、选中隐藏类型、详情、来源链接、局部展开、缩放、画布平移、节点拖动、重置和 390×844 手机布局。控制台无相关错误或警告。
- 真正空数据（0 节点）与 2,000 节点／1,999 条明确样例关系通过打开、搜索末尾记录、局部聚焦和继续展开检查；后者生成 HTML 约 1 MB，布局与打包约 1.35 秒。样例不进入正式档案。
- 停止 HTTP 测试服务后，已加载页面继续搜索和重置成功，确认交互无需后端请求。直接 `file://` 打开仍被内置浏览器 URL policy 限制，未绕过，不能把上述检查等同为直接文件打开验收。
- 本地 Node 语法检查通过。公共 Git 检查阻止可检测秘密和禁止公开正式记录；正文文件的符号链接也不会被阅读视图收录。
- GraphQL profile 与列表批量读取通过稳定数字 ID、旧 REST 断点保留、游标恢复、不完整页重放、桥接边界和限流测试。修正了拥有仓库数为零不等于全部公开参与仓库为空的问题，相关旧完成任务重新排队。
- 对真实 GitHub 两个小账号的仓库列表进行了隔离集成读取；后续在正式研究数据库续跑列表，并根据服务端资源限制缩小同类批次。

## 研究状态与原件

初始迁移完整保留 43,298 项 pending、21 项 missing、4 个未完成分页与 0 轮无新增。迁移源 `state.json` 共 42,886,349 字节，SHA-256：`d0c4ffa9c2e238f336e2b7791e58e0710c1ab0b2d6890e0fe5b4533e1423c486`。

旧批次共 8 个文件、95,351,288 字节完成哈希清点，原文件不改写。新状态仅写入本任务 `.work/discovery/`。续跑过程中存在新候选与新分页，队列没有收敛；研究结果保持 `partial`，不得将隔离样例或未经复核的候选作为正式档案发布。

## 仍需完成

- 完整处理规则内可执行队列、必要分页、缺失 README 和人工复核，进行两轮定向无新增补漏。
- 复核全部研究交付与正式入库输入，保留稳定来源定位与研究映射。
- 将真实成果入库，并实际验证图谱、SVG、Markdown 的一致性与浏览路径。
- 完成跨平台 CI、仓库更名、推送、PR 合并、Pages 部署和服务端核验。
- 回写 Issue #2 与 Issue #1 的实际结果。

## 后续核查

- `5c58e75` 后完成：公开凭据检测的词边界修正，避免误删 `How-To-Ask-…` 等仓库名及 Git blob 哈希；研究区复扫 123,544 行数据库记录／关系／操作／审计内容，共 80,514,486 字节，无凭据模式命中。扫描结果是模式检查，不替代逐条证据复核。
- 明确仓库转为非公开后，不继续读取 README 或贡献者；REST 断点在数字身份验证后使用新账号／仓库路径，保留原页码。
- 原 8 个研究输入再次核对哈希，全部不变。
- 3 条确认复核（2 个独立校园资源及 1 条二级目录）已写回研究区；4 条单独复核和 29 条基于相同 README blob、默认分支比较的重复 fork 复核保留完整出处。仍有差异的 fork 不自动排除。
- 已准备 12 条待入库记录（4 Knowledge、1 Event、1 Node、1 Collection、5 Source），只存在于私有研究输入及隔离审阅目录；Schema、引用和公开投影通过。来源固定到 Git 提交，Source ID 包含原文 blob 版本，未写入正式档案区。
- 发现旧队列将 GitHub `claude`（数字 ID 81847，公开主页指向 Anthropic Claude Code）作为校园贡献者扩展起点。已询问工具／自动署名账号是否停止社交扩展；尚未收到选择，没有据此删除旧队列或修改扩展政策。等待期间继续已有候选的资料、仓库、星标和证据读取。
- 真实旧分页中的 `/user/<id>/repos` 路径已通过数字身份匹配兼容，继续原第 21 页；同时覆盖组织与仓库的数字路径，并拒绝其他实体 ID。
- README 批量读取使用固定提交、完整字节数与 Git blob 哈希；7 个真实仓库与 REST 的路径和哈希逐项一致，另 1 个存在选择歧义，保持 pending 转回 REST。隔离测试未改变正式内容。
- 后续 6 条完整 README 范围复核已写回研究区：3 条确认（含暂不重复计数的助手项目），3 条排除仅账号、邮箱或通用课程示例关联的候选；保留判断理由、原文定位与未核验事项。

## 9 月 6 日续步检查点

- 单个 GraphQL 列表查询超时后可以缩小单页数量，原游标和累计已读项不变；限流仍保存退避，未通过缩小请求绕过。新增参数验证拒绝将导入状态中的任意字符串拼入查询。
- UOD-SZTU-2025 已补充研究组论文页面、固定提交中的原始 Markdown 与 IROS 议程证据。保留议程旧仓库地址返回 404 的差异，不合并未经证明相同的账号。
- 又完成 121 条默认分支无新增提交的 fork 复核；上游实体与其阅读队列保留。有差异、不可访问和本次未读到的候选继续待复核。
- 隔离审阅集扩展到 15 条真实记录（5 Knowledge、1 Event、1 Node、1 Collection、7 Source）和 26 条档案关系。HTML、SVG、Markdown 使用相同版本 `sha256:ca75ec52261653b75ada33d58562f85c8c3d8883f83f2a96cfe788c406eb64f9`，连续构建一致，121 个相对链接有效。仍未正式入库。
- 通过只服务此审阅 HTML 的本地 HTTP 页面，实际核对中文搜索、档案详情、来源和固定提交链接；单独打开来源页面成功。内置浏览器点击新窗口链接后未出现新的受控标签页，不将此等同于线上 README 完整跳转验收。停止测试服务后，已加载的真实档案仍能聚焦，控制台无相关错误。
- 修正有效期未知的展示文字，避免将其误读为整条档案没有核查证据；更新后的页面已实际核对。
- 研究数据库扫描 157,311 行，共 112,238,039 字节；研究导出与隔离审阅产物共检查 74 个文本文件、154,991,252 字节，凭据模式无命中。旧 8 个输入哈希再次核对，全部不变。
- 标准隔离 wheel 构建通过，新旧入口模块与全部查看器资源存在。首次尝试关闭构建隔离因虚拟环境没有 setuptools 失败，改用项目声明的标准隔离构建后成功。
- 当前研究快照：23,598 项 complete、50,570 项 pending、238 项 missing、135 项 restricted、131 个未完成分页，连续无新增轮数仍为 0。confirmed 数量包含账号等研究实体，不等于独立校园项目数。
- 研究执行和本地测试服务已停止在可恢复断点；工具／自动署名账号的社交扩展范围问题仍待用户答复。尚未更名远端、推送、创建或合并 PR、发布 Pages、回写 Issue。服务端 main 仍为上述基线。

## Checkpoint

- `abc8e77d8fa2fa8f23031cd00b1f31b195f51bac`：更名兼容、v0.2 模型、研究工具、三种档案视图与 Pages 构建的首个工程检查点。
- `5c58e75`：公开检查、图谱交互修正、批量研究读取和实际浏览验证。
- `355d171`：身份、访问范围、fork 原始来源和凭据模式修正。
- `9a760a0`：数字分页兼容、README 批量证据读取及 macOS 实际安装验证。
- 下一检查点保存单页自适应恢复、真实档案浏览校验与本节阶段记录。

本文件是阶段记录，不是最终完成证明。
