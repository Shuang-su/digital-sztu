# 执行计划

1. 修复所有现存 worktree 的移动路径；保留旧环境，在当前路径按锁文件重新安装。主分支工程修复使用独立 codex/runtime-recovery 分支；研究工具继续在既有 codex/digital-sztu-upgrade 分支实现。
2. doctor 检查失效 worktree 指针、虚拟环境来源与研究存储空间。保留源码和研究数据库，不自动清理系统盘。
3. 对研究数据库创建一致性备份，先在副本迁移索引，逐表对比记录、操作、关系、元数据及审计哈希，再应用真实迁移。SQLite 使用每连接内存排序；GitHub 子进程使用数据库旁暂存目录。只读 status/inspect 不迁移或写入。
4. SQL 内执行队列类型筛选、稳定排序与单次取数限制；添加事务索引、stderr JSONL 进度、Ctrl+C 退出码 130、空间不足回滚及恢复命令。inspect 聚合证据与缺口，不自动确认候选。
5. 公开凭据条款与验收第 2 项存在矛盾，执行开始时已说明处理边界：公开姓名和必要上下文保留，不公开可用于接管第三方账号的 Token、Cookie、私钥等信息，不实现访问控制绕过。保留本地默认扫描的复核提示；严格扫描阻断可检测凭据；主分支 CI 加入公开正式记录检查及严格扫描。大文本逐行扫描，报告不回显命中值。
6. 运行相关测试、完整测试、归档校验和两次确定性构建，按工程阶段保存 checkpoint，推送并在 CI 通过后合并独立主分支修复。继续真实普查，按实际状态保存进度。
7. 完整普查、必要分页、证据复核及连续两轮无新增补漏结束后，才正式入库、合并完整 v0.2 并发布 Pages。沿用同一公开数据生成三种视图，最终核验 README 到来源的路径。三栏 UI 与 RAG 等继续作为未来需求。
